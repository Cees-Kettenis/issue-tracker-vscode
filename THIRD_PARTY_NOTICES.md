# Third-Party Notices

This project includes development dependencies with the following licenses:

1. TypeScript (`typescript`) - Apache-2.0
2. Node.js Type Definitions (`@types/node`) - MIT
3. VS Code Type Definitions (`@types/vscode`) - MIT

These dependencies are used for building and type-checking the extension.
